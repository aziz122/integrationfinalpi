

{% extends 'front.html.twig' %}
{#
{% block title %}Commande{% endblock %}

{% block body %}
    <h3>Liste Commande</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Prixtot</th>
                <td>{{ commande.prixtot }}</td>
            </tr>
            <tr>
                <th>Datecomm</th>
                <td>{{ commande.datecomm ? commande.datecomm|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Modepaiement</th>
                <td>{{ commande.modepaiement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_index') }}">back to list</a>

    <a href="{{ path('commande_edit', {'id': commande.id}) }}">edit</a>

    {{ include('commande/_delete_form.html.twig') }}
{% endblock %}
#}
{% block body %}
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>

<h2 style="text-align: center;background-color: #5cb85c">{{ "Your Order Is Registered !!" }}</h2>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
{% endblock %}